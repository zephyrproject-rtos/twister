common:
  tags: kernel posix
  extra_configs:
      - CONFIG_POSIX_API=y
  min_ram: 32
tests:
  xyz.common_merge_1:
    tags: picolibc
    extra_configs:
      - CONFIG_NEWLIB_LIBC=y
    min_ram: 64
  xyz.common_merge_2:
    tags: arm
